# 生产工艺流程追溯系统设计规范

> 本文档只约束管理端的视觉语言和通用交互，不规定具体页面、菜单、业务模块或状态字典。
> 页面功能和业务流程以最新需求、API 契约和数据库设计为准。

## 1. 设计定位

### 1.1 风格

**Clean Industrial Admin / 清爽型工业管理后台**

关键词：清爽、克制、表格优先、弹窗表单、状态清晰、适中的信息密度、低装饰、低动效。

不采用大屏炫酷风、重型 ERP 灰蓝风、官网营销风，以及过度科技感或阴影。

## 2. 色彩规范

| 用途     | 色值      | 说明                       |
| -------- | --------- | -------------------------- |
| 主色     | `#283A50` | 侧边栏、标题强调、核心导航 |
| 辅助蓝   | `#306188` | 主按钮、链接、选中状态     |
| 强调黄   | `#EAB321` | 少量步骤或重点强调         |
| 页面背景 | `#F5F7FA` | 页面整体背景               |
| 卡片背景 | `#FFFFFF` | 表格、表单、弹窗主体       |
| 边框灰   | `#E5E7EB` | 表格线、输入框边框、分割线 |
| 正文黑   | `#1F2937` | 主要文字                   |
| 次级灰   | `#6B7280` | 辅助说明                   |
| 弱灰     | `#9CA3AF` | 占位符、弱提示             |
| 成功     | `#22C55E` | 成功、完成、正常状态       |
| 警告     | `#F59E0B` | 待确认、部分完成、风险提示 |
| 错误     | `#EF4444` | 失败、异常、危险操作       |
| 信息     | `#3B82F6` | 进行中、信息提示           |

使用原则：以白色、浅灰和深蓝为主；黄色仅作少量强调；危险操作使用红色或危险样式；同一状态语义在全系统复用相同颜色；表格状态优先使用标签而非大面积色块。

### 2.1 异常与时间风险的可视层级

执行、任务和处置类页面必须让用户无需逐行阅读即可定位问题。可视风险只能由服务端事实或可稳定派生的日期、数量和状态产生，不得为凑效果伪造“异常”、“延误”或进度。

- 已逾计划日期但尚未完成的对象使用警告语义：显示计划日期和逾期天数，可将对应卡片或行的局部边框改为警告色。
- 存在有效异常数量、失败事实或待处置阻断时使用错误语义：数量和关键文字使用红色，可将最小可定位容器的边框改为错误色。
- 同时存在多种风险时按错误高于警告的优先级表达，并保留“逾期 N 天”、“待处置 N 项”等明确文字；不能只靠颜色传递信息。
- 同一列表中的不同业务状态必须具有可辨认的颜色或明暗差异，禁止把待处理、已派工、执行中、已完成、已取消等多个状态统一成同一种标签颜色。状态色表达业务阶段，异常/失败色表达风险；当两者同时存在时，风险使用更鲜明的错误色和局部边框，不能被普通状态标签弱化。
- 进度使用“已完成 / 总数”和进度条共同表达；异常、逾期和进度应放在识别信息附近，不得隐藏在详情底部。
- 强调范围应克制：优先边框、标签、数字和浅色底，禁止大面积高饱和背景或闪烁动画。
- 列表需要跨多条记录显示进度或异常数时，应由列表查询投影一次返回；不得为每行发起详情请求造成 N+1。

### 2.2 报废补产的路线表达

- “报废并补产”确认弹窗必须同时展示异常来源工序、异常数量、补产起点、受影响工序路径和激活条件。当前补产起点固定为路线首工序，使用只读文字或标签表达，不伪装成可编辑下拉框。
- 补料数量与产品补产数量分区展示：物料表格只表达管理员选择的物料和数量；产品补产摘要固定展示来源报废数量。禁止把某种物料数量、物料数量合计或 BOM 换算值展示成产品补产数量。
- 补料未全部确认领用时使用“待补料/待领料”状态和解释文字，不启用补产报工入口。激活后，在生产执行页对受影响工序展示“批次计划 + 下游报废补产”的目标拆分、当前正常量、投入放行量和剩余可报量。
- 因下游报废而重新打开的上游工序使用“补产重开”标记，并在工序识别信息附近展示来源工序和数量；来源工序在前道新增正常产出到达前显示“等待前道补产放行”，不得显示误导性的可报数量。
- 所有目标量、放行量、补产来源和可报量由列表/详情投影一次返回，前端不得自行遍历报废记录计算，也不得为每道工序额外请求详情。

## 3. 字体与排版

```css
font-family: Inter, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', Arial, sans-serif;
```

| 场景     | 字号 | 字重 |
| -------- | ---: | ---: |
| 页面标题 | 20px |  600 |
| 区块标题 | 16px |  600 |
| 表格正文 | 14px |  400 |
| 表单标签 | 14px |  500 |
| 辅助说明 | 12px |  400 |
| 状态标签 | 12px |  500 |

页面内容边距为 24px；卡片内边距为 16px 或 20px；表单垂直间距为 16px；按钮间距为 8px；工具栏与表格间距为 12px。

## 4. 管理端布局

管理端使用固定后台结构：左侧菜单、顶部栏和内容区。顶部栏根据路由元数据显示当前页面名称，并承载系统级信息、当前用户和退出操作；不要堆放业务操作按钮。

左侧菜单建议宽度 220px，最多两级，使用轻量线性图标；当前菜单使用深蓝背景或蓝色左侧高亮。实际菜单项由路由和权限配置驱动，不在本规范中固定。

常规业务页不在内容区重复显示与顶部栏相同的页面标题，内容区优先从筛选区、工具栏、表格或主体卡片开始。说明文字只用于长期有效的业务规则、风险或操作帮助，不得使用“迁移验证”“测试入口”等开发占位说明。业务需要时可使用详情区域或卡片分组，但不绑定具体页面类型。

## 5. 弹窗规范

新增、编辑、查看详情、分配、确认和上传等操作统一使用 **Modal**；不使用 Drawer 作为主要交互方式。

弹窗按内容复杂度自适应，最大宽度不超过 `75vw`、最大高度不超过 `85vh`。内容溢出时只在弹窗内部滚动。

| 类型       | 推荐宽度 | 最大宽度 |
| ---------- | -------: | -------: |
| 小型确认   |    420px |     90vw |
| 常规表单   |    640px |     75vw |
| 多字段表单 |    860px |     75vw |
| 复杂详情   |   1000px |     75vw |

```css
.dialog-sm {
  width: min(420px, 90vw);
}
.dialog-md {
  width: min(640px, 75vw);
}
.dialog-lg {
  width: min(860px, 75vw);
}
.dialog-xl {
  width: min(1000px, 75vw);
}
.dialog-body {
  max-height: 70vh;
  overflow-y: auto;
}
```

弹窗底部取消按钮在左、确认按钮在右；确认文案必须表达实际动作。删除、取消、关闭、作废等危险操作必须二次确认；关闭已修改的表单时应提示是否放弃修改。

### 5.1 路由区域与多标签页缓存

业务弹窗的遮罩和交互范围只覆盖当前路由内容区，不得覆盖左侧菜单、顶部栏和多标签页栏。弹窗打开期间，用户仍可通过左侧菜单或标签页切换到其他路由，以补充当前操作依赖的前置数据。

编辑弹窗属于路由页面的工作状态，可以随 Vue KeepAlive 缓存保留。离开页面时不主动关闭弹窗，也不清除普通输入、已选值、校验状态和未提交内容；返回原标签页时恢复离开前的编辑现场。弹窗 DOM 必须保留在对应路由组件子树内，缓存页失活后不得继续显示或拦截当前页面操作。

危险操作的二次确认框同样只覆盖路由内容区，但属于短时确认状态，不随页面缓存；用户切换路由时应自动关闭，返回后由用户重新发起确认。

依赖其他主数据或实时业务状态的候选项不得以页面缓存作为唯一数据来源。部门、角色、产品分类、关联产品、物料、工序、工艺路线和负责人等候选数据应在以下时机重新请求：

- 首次进入页面；
- 打开相关编辑弹窗；
- 缓存页面重新激活；
- 用户展开需要实时数据的选择控件或主动点击刷新。

刷新候选项不得覆盖普通输入、已选值或表格型编辑草稿。原已选值在最新候选集中已停用、删除或不再满足条件时，应继续显示为“已失效”，禁止把内部 ID 当作正常标签展示；保存前必须要求用户重新选择，最终提交仍由后端重新校验权限、状态、版本和业务前置条件。候选项合并只保留当前已选失效值用于显示与提交拦截，未选中的失效项不得残留在下拉列表中。

### 5.2 前端交互基础设施

路由弹窗、短时确认框和动态候选项统一使用管理端基础设施，不得由业务页面重复实现挂载、遮罩或失效值规则。

| 基础设施          | 使用场景                                                     | 强制要求                                                                                |
| ----------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| `RouteDialog`     | 新增、编辑、详情、分配、上传等需要随标签页恢复的 Dialog      | 业务页面继续使用全局 `<el-dialog>`；不得设置 `append-to-body=true` 或直接绕过全局适配器 |
| `RouteMessageBox` | 删除、停用、取消、作废、完成等短时危险操作确认               | 不得直接调用 Element Plus `ElMessageBox`；路由切换时关闭且不缓存                        |
| `live-options`    | 部门、角色、分类、产品、物料、工序、路线、负责人等可变候选项 | 在页面激活、弹窗打开或下拉展开时刷新；保留并标记失效已选值，提交前拦截                  |

稳定枚举、普通输入和不参与编辑提交的展示数据不使用 `live-options`。具体交互接入、生命周期和测试要求见 [`DynamicTabSwitching.md`](docs/frontCompoentDesgin/DynamicTabSwitching.md)；候选的数据所有者、实例边界、失效和竞态规则见 [`frontend-architecture.md`](docs/frontend-architecture.md)。

## 6. 表格规范

表头使用 `#F9FAFB`，边框使用 `#E5E7EB`，建议行高 48px，悬停行背景使用 `#F3F4F6`。关键识别字段可加粗。

列顺序遵循：识别字段 → 名称/型号 → 分类/类型 → 数量 → 状态 → 负责人 → 时间 → 操作。行内操作应保持克制，超过四项时收入“更多”；危险操作进入“更多”或二次确认弹窗。

## 7. 表单规范

简单表单使用单列，中等复杂度表单使用两列，复杂表单通过通用分组标题组织。必填项必须清晰标识。

表单和危险确认不能只展示输入控件或动作名称。用户提交前应同时看到当前对象的关键识别信息、数量/日期约束、动作将产生的业务副作用，以及不可撤回或后续受限的结果；这些提示必须来自当前真实合同，不得从旧页面补入未实现字段。

| 数据类型  | 优先控件       |
| --------- | -------------- |
| 文本      | Input          |
| 长文本    | Textarea       |
| 枚举/分类 | Select         |
| 数量      | InputNumber    |
| 日期      | DatePicker     |
| 日期时间  | DateTimePicker |
| 文件      | Upload         |
| 布尔值    | Switch         |
| 多选      | CheckboxGroup  |

接口成功后的新增、编辑、删除、确认、上传、下载等操作必须经 `EMessage` 提示成功；失败时必须显示可理解的失败原因。查询列表可保持静默。

## 8. 图标、圆角与空状态

使用轻量线性图标，不使用复杂插画。卡片圆角 8px，按钮和输入框圆角 6px，弹窗圆角 10px；卡片使用轻微或无阴影，弹窗可使用中等阴影。

空状态应包含简短提示、可选说明和一个与当前上下文相关的主要操作；不硬编码未来业务模块或功能名称。

## 9. 响应式与交付检查

一期优先适配桌面端，主要适配宽度不低于 1440px，最低支持 1280px；窄屏下表格允许横向滚动，不优先为手机端重构管理端。

页面交付前检查：

- 是否使用统一色彩、字体、间距和状态语义；
- 是否使用 Modal 而非 Drawer；
- 是否为危险操作提供二次确认；
- 是否为业务操作提供 `EMessage` 成功/失败反馈；
- 是否避免把具体页面规划、菜单规划和业务状态字典写入本规范；
- 是否检查中文文案为 UTF-8，且不存在乱码。
