# 路线图

本页只维护未完成实施、待决策及待验收事项。正式测试须等用户明确通知，再按 [AGENTS.md](../AGENTS.md#数据库与交付约定)由 Luna MAX 子代理全量验证；应用类型、构建与启动通过不代表用户验收或正式测试通过。当前规则从模块所有者读取，ADR 用于追查理由与取代关系。

## 采购到入库与来料整批处理：待验收

当前规则见 [Procurement 正式清单](../apps/api/src/modules/procurement/docs/receipt-acceptance.md)、[数量示例](../apps/api/src/modules/procurement/docs/receipt-round-examples.md)、[Quality 模型](../apps/api/src/modules/quality/docs/unified-inspection-model.md) 和[管理端交互](../apps/admin-web/docs/incoming-inspection-adoption.md)。实现已接入，以下场景仍待验收。

### 黑盒与 UI 验收

- [ ] 联合验收供应商名称维护 → 单工单按需求／独立备料采购 → 多次到货 → 全检／抽检 → 库管定稿 → 分次入库 → 生产分配及领料；核对分页、跨页选择、历史来源与权限失败交互。
- [ ] 验收同工单跨任务合单、跨工单提交拒绝、逐行供应商、同版本不同供应商拆行、需求逐条映射及独立采购量；需求更正／关闭不改写采购，采购和入库不直接减少需求，补单不回写原单。
- [ ] 验收采购列表默认折叠不读取详情、块头主单操作／行内结束、供应商仅逐行展示、进行中语义、外部定位、展开／收起／翻页／缓存页返回、读取失败与迟到响应、动作前依据变化、未知结果原键重试。
- [ ] 验收 m1/x1 各10独立处理、m1误定稿11后更正10；原报10／实测12的全检与抽检、有依据更正归零；更正输入本次到货核实总量T，T不得小于已入I＋已退B，原实收少录时可独立更正增加经核实的同次剩余实物，真实新来货和损耗不能冒充实收更正。更正到零不等于从未到货。
- [ ] 验收明确发起检验后可返回继续录入结果；来料质检只填G/F及结论，库管核实C；全检与抽检的差异提示、超建议可入依据、N>C或C<F时无有效建议且须说明。核对校准不强制重复检验，不覆盖检查事实；待复检／不放行不能被数量例外绕过。成品由质检核实C、产线核对并送负责人审批；质检计算仅作数量提示的当前实现待验收，固定基准和复检开始冻结另见CQ-01，不将来料库管职责或强制超建议说明套用于成品。
- [ ] 验收整批更正、复检和重新定稿：100拆96/4后全部未处置分配一起暂停；已入60、更正累计102只重办42；其他到货明细独立继续。旧轮分配无操作资格，最新轮替代旧轮，不累加历史清单；纯分配更正不得重新授予已消费量。
- [ ] 验收各未处置阶段整批人工拒收、待检直接拒收后无QC实际退回、检查中拒收使迟到结果409且不新增事实；拒收不是实际退回，也不取得质量补发资格。每个待退分配一次完整交接，已入／已退事实保持不变。
- [ ] 验收撤销人工拒收：原因必填，同量不新增revision，正剩余回待检且不恢复旧分配，全部已退不可撤销；拒收后更正实收同样回待检，仍需拒收须再次决定。经过已定稿无分配的零剩余轮后不恢复更早授权。
- [ ] 验收计划100／到货120／QC118+2三种分配：原单118＋质量待退2；原单100＋超量补单18＋质量待退2；原单100＋超发待退18＋质量待退2。区分免费超量与额外购买；补单未下单先留待处理，原单可先入，补单承接不重复到货、检验或建批次。
- [ ] 验收补单归属跨轮保留、净差量显式核对、整批拒收及归零；补单历史一次性绑定不得重用，采购终止约束不得经复检解除。质量／超发／终止退回与原单／补单分别追溯、独立履约及人工逐行关闭；采购关闭不妨碍原到货合法检验、更正、入库和退回。
- [ ] 验收质量补发先补后退、已退后补发、原单关闭与补发独立；核对有效正式质量待退分配及供应商约定、旧处置被更正／复核后的创建与下单拒绝、补发单独立到货／检验／定稿／入库，保留真实退回和未知结果重试。
- [ ] 验收同一allocation分次入库、同供应商多个有效分配同次入库、首次自动生成原内部批号、同到货后续沿用批次、历史经inbound_detail追溯；核对旧页409、同键重放、无采购来源旧写入口关闭及整体回滚。
- [ ] 回归 Inventory 提取后的生产需求、预留、领料、退料、盘点和批准成品入库；停用精确版本允许采购入库，但不得生产领用或计入可领齐套量，历史库存总量仍保留。成品继续按批准清单每类收齐一次确认。

### 正式测试阶段（等待用户明确通知）

- [ ] 正式测试阶段迁移旧 scope／质量夹具，补相邻单元／组件、API 及 MySQL 集成覆盖。
- [ ] 覆盖六表及统一Quality同源FK／条件约束、质量补发迁移202609210003、整批轮次与allocation迁移及相关成对migration up/down/up和非空保护；覆盖实收归零、拒收撤销、采购归属及终止继承、数量授权与所有权、权限、并发、幂等、回滚和上述生产回归。
- [ ] 补采购列表相邻组件与请求生命周期用例，覆盖取消读取、迟到响应、版本变化及未知结果重试；端到端场景须先满足测试策略中独立环境、数据准备和可运行入口的前提，不将页面冒烟称作E2E。

### 后置采购扩展

工单提前采购、按需与备料混合来源单、跨到货合并检验、供应商打印、门口拒收登记及抽样分档配置仍待另行安排。一期继续使用独立备料、分入口下单、按到货明细检验及实际抽检数量录入，不扩大为完整 ERP／MES、财务、供应商评审或生产在线质量。

## 任务用料、研发与成品质检：待验收

现行规则见[任务设计](../apps/api/src/modules/production/docs/database/work-orders-and-batches.md)与[成品检验](../apps/api/src/modules/quality/docs/finished-inspections.md)。

- [ ] 验收同一批量工单不同任务独立版本、每任务每物料单版本、初配供应商提示冻结、人工提需／工序补料／更正不能绕过锁版，工单旧配置入口退出；停用版本拦截、并发初配防重，换版通过新任务表达，原任务按进度取消或结案，已终止计划不占额度且历史需求及产出保留。
- [ ] 验收研发无需 BOM、默认路线不自动继承、首次与后续手工提需、自由选择新物料及版本、旧剩余独立关闭；真实需求/领退料身份和数量仍不可覆盖，开工保留齐套或有效短批条件，结束不要求工序报工。
- [ ] 验收批量与研发损坏只留事实、不自动提需或补料，已确认损坏永久占用可退额度；人工额外用料独立决策，工序异常报废补产仍沿原流程。
- [ ] 验收 Quality 成品质检独立页面与后端所有权，全检/抽检/零量核实、实检和整批放行、复检链，Production 公共能力引用、当前数量建议与实际清单差异提示及历史、计划内外重新划分、超计划产出、已入库类别锁量及完整审批证据。
- [ ] 验收成品质检独立填写合格／不合格数、抽检另核实实际送检总数，移除剔除量及范围说明输入；原申报 10、实物 12 时，全检 11 合格／1 不合格和抽检总数 12、样本 4 合格／1 不合格，明确放行后本轮建议均为11；数量建议交互待用户验收。核对差异提示、管理员修正清单、待复检阻断和不合格不自动报废，最新整批复检不叠加历史建议。管理员修正为计划内10／计划外1不重复质检；已有工序报废1时，不合格1仅在实际决定报废后才使总报废成为2。
- [ ] 正式测试阶段迁移旧夹具，覆盖任务 API、NULL BOM 字段、任务锁版组合 FK、幂等、权限、并发、事务回滚及 migration up/down/up。

### CQ-01：成品剩余复检整改

状态：成品质检数量只作建议，相关后端门禁与前端提示已接入，待用户黑盒／UI验收及正式测试。固定已入基准、剩余范围快照和复检开始冻结尚未实现。决策见[ADR-0015](adr/0015-unified-quality-quantity-semantics.md#成品数量与职责)，差异见[CQ-01](documentation-conflicts.md#cq-01)，操作见[手册](manuals/finished-output-inspection-inbound.md)。

- [ ] 验收当前非阻断数量建议：尚无已入时比较计划内＋计划外与本次建议；已有入库时分列当前已入量与本次建议，不用动态值伪造固定I₀或累计建议。差异不阻断保存、送审或批准，不新增强制超建议说明或额外审批；保留原说明、负责人审批及检验／建议／最终清单历史。
- [ ] **数量建议整改待补正式测试**：覆盖清单A＋E及历史已入量超过本次建议仍可送审／批准；pending_reinspection／not_released继续阻断新送审和批准（包括零清单）；历史not_released零量审批证据可读取但不授予写资格；前端仅提示差异且不禁用提交、不增加超建议必填；计划内上限、已入类别锁量、有效正式批准量及防重复入库保持。当前类型、构建、架构和启动验证不代表上述正式测试或用户验收通过。
- [ ] 补齐明确的复检开始动作及来源基准：共同事务内固定本轮已入量I₀、剩余送检身份与版本，并暂停全部剩余送检实物入库；仅打开前端表单不算开始，旧批准版不能绕过冻结。
- [ ] 调整检验来源快照、累计提示及审批证据：以固定I₀及本轮R形成累计建议S=I₀＋R，记录对应范围和版本；累计计划内＋计划外偏离S仍只提示。不能用浮动已入量或历史建议重复形成累计依据。现有数量建议调整无表／契约结构变化；后续若需要结构变更，只追加成对migration。
- [ ] 串联复检、清单更正、撤回／驳回、取消更正与新批准版生效后的入库资格；待复检、不放行或尚无有效新批准依据时，不能通过取消更正或旧单恢复被暂停的入库资格。历史检验、批准版及实际入库关联保持不变。
- [ ] 调整前端阶段提示和定位，明确“先发起清单更正”路径、默认待检筛选范围及当前冻结状态；同步更正确认文案，在固定基准接入后展示I₀、累计建议及实际定稿差异。保留计划内、计划外各一次入库，不引入在库质量、成品出库或库存冲销。

## 需求纠错与逐项收尾：剩余验收

规则见 [ADR-0010](adr/0010-demand-correction-by-replacement.md)、[需求所有者文档](../apps/api/src/modules/production/docs/database/demand-allocation-and-outbound.md#正式需求更正与替代) 和[收尾设计](../apps/api/src/modules/production/docs/database/production-termination.md)。专项验收以以下条目为准。

两条主链已有用户黑盒反馈，未发现新的阻断问题；允许继续准备后续阶段，但下列专项仍未完成，正式测试继续等待明确通知。

- [ ] 随用户反馈修正 UI，保持“提前结束 → 继续收尾 → 查看结案信息”的操作语义；工单下达审批仍不属于当前已接入场景。
- [ ] 黑盒验收收尾预留筛选及需求关闭：已全部领料的正常有效分配不列为释放待办、不阻断剩余需求关闭，物料实核及领料追溯仍完整；部分领料仍须释放剩余预留，冻结／异常分配及未完成出库仍须核对。用户明确启动正式测试阶段后补回归测试集。
- [ ] 用户黑盒与 UI 验收：原需求 10 已领 4 改总量 7 → 新剩余 3；连续更正累计整链已领；零剩余和普通关闭；单据零领料不误激活、多需求及多补料单独立齐套；原方案和领退料、旧新需求、审批追溯；在审旧需求冻结与其他生产继续；驳回／撤回、重复提交、并发出库和最后一级审批。
- [ ] 用户验证逐项收尾：已领料批次进入 closing，分别处理待出库、预留、需求、工序、异常、返工、补料与物料安排，保存实际产出后送审；审批人看到处理结果与数量，驳回不自动恢复中间态，末级通过才 terminated，全部批次终态后关闭工单。覆盖全部报废、零产出、历史报废不重复、待确认退料／损耗、事实变化失效、工单产出汇总和缓存刷新。
- [ ] 正式测试阶段覆盖 MySQL up/down/up、非空回滚保护、类型与权限、幂等／并发／事务回滚及上述业务分支。

## 任务统一结案与产出清单：待验收

当前流程及实现边界见[结案所有者文档](../apps/api/src/modules/production/docs/database/production-termination.md)与 [ADR-0011](adr/0011-task-closeout-output-list-and-finished-goods-inbound.md)。

- [ ] 联合验收草稿修改、线下检验后改量、工单负责人审批、正常／短产／提前结案、零产出和全部报废、清单打印、更正及旧版失效；同一流程版本下不同工单分派不同负责人，非负责人即使有审批权限也不能处理，缺人／资格失效阻断，发布新版不改变在途人员；同时覆盖申请人自审、同人跨级、未知业务人员来源拒绝，业务来源只对已声明支持的场景开放，BOM和需求更正原角色／指定用户配置不被误加工单负责人限制；分页／详情／通知人员一致、驳回／撤回保留事实、原人员资格恢复后继续，以及旧浏览器不能误批新节点。正式测试阶段补相应回归。清单生效／冻结与入库确认的并发联动在下节一起验证。

## 成品入库、研发轮次与工单编号：待验收

成品入库的身份、单据、账本及每类一次确认规则见[入库所有者文档](../apps/api/src/modules/production/docs/database/finished-goods-inbound.md)，以批准清单为依据。已有批次结束能力见[批次结束设计](../apps/api/src/modules/production/docs/database/production-termination.md)，最终入库边界见 [ADR-0011](adr/0011-task-closeout-output-list-and-finished-goods-inbound.md)。

- [ ] 用户黑盒与 UI 验收：批准清单查询／打印、两类入库及库存追溯、已结案待入库、零可用不入库、清单更正前后数量与审批追溯、旧版及在审拦截、并发／重复确认；计划 10、可用 8、报废 2 时缺口仍为 2，可用 12 时计划内 10／计划外 2。历史报废不重复计入，并覆盖研发轮次关联、工单自动编号、每类一次确认及已入类别禁止改量。
- [ ] 用户验收结案物料损坏“不补料”登记：正常／提前结案、已关闭需求和已释放分配的原领料来源、待确认退料／损耗占额、超量和过期核对拦截、登记后重新实核、审批及打印逐笔依据、驳回保留损坏事实与永久可退占用；不改变库存、物料计划或生成补料补产。在产损坏同样不自动补料，额外需求独立手工提出。
- [ ] 正式测试阶段同步旧批次测试夹具与断言：批次不再持久化完成／合格计数，任务／追溯／执行结果使用 `lastStepReportedQuantity` 和当前批准 `finalOutput`，工序正常量使用 `normalQuantity`，工单候选增加额度与批准汇总，批次创建幂等 scope 使用 v8。覆盖清单更正后工单与任务显示一致、已入库单类锁量、终止任务已有报工与审定量分别展示、创建时额度变化不覆盖草稿；分别通过 API／管理端 `typecheck:test` 与正式测试，不将应用构建当作测试集通过。
- [ ] 正式测试阶段覆盖独立终止报废不补料补产、不改变库存或报工、收尾/纠错审批、同事务回滚、权限/幂等/并发、全部批次终态后结案及本节新功能。

## 全部成品入库完成后：补料管理

执行前置：统一结案与产出清单、清单更正、成品库存身份、生产流转入库、`production_extra` 额外产出入库及确认防重等任务全部完成，并按每类收齐后一次确认的规则验收后，再实施本节。当前仅记录排期，不直接开发补料管理。

- [ ] 在“生产管理 → 补料管理”提供“补料单”和“领料损耗申报”两个页签。正式补料单复用 `production_material_supplement`，展示 `step_scrap_reproduction` 工序报废补料，领料损坏另行记录且不生成补料单，支持业务来源、工单、任务及履约状态筛选。
- [ ] 从当前报废页整体迁入领料损耗申报、确认、取消和历史查看；待确认损耗仍属于申报记录，确认后只形成正式损坏事实，不生成补料单。迁移完成后移除仅承载该功能的旧菜单，旧路由跳转至新入口。
- [ ] 补料单详情展示产生原因、来源记录、原始需求、当前有效需求、替代历史、审批追溯、分配及领料进度；数量按物料和单位分别展示，不跨物料汇总。
- [ ] 领料损耗页签提供全部损耗单的分页查询、状态筛选和详情，覆盖待确认、已确认、已取消，并区分在产损坏和结案损坏用途；两者均不关联补料单，独立手工提需不改写损坏事实。以可复用只读详情弹窗串联来源领料／分配／需求、损耗、补料及替代链，并从退料的损耗占用进入同一来源明细。现有生产追溯页承载按任务汇总的关联查询；单据详情使用所属管理页的 URL 查询参数定位弹窗，不为每条关联新建路由或菜单。跨页操作返回时保留来源筛选及草稿，目标接口独立鉴权；当前没有损耗冲销能力，不提供撤销已确认损耗入口。
- [ ] 工序报废及产品补产授权继续从异常处置产生，补料管理承接后续物料处理和追溯；损坏不生成需求或补产额度；人工追加仍属于追加需求，不自动归为补料单。不新建第二套需求或库存账本。
- [ ] 补齐统一查询、路由及页面权限，复用既有申报、审批、更正、分配和出库命令；按既定顺序先做黑盒与 UI 验收，再补正式测试集。

## 补料管理接入后：废品入库

执行前置：上节补料管理及领料损耗统一查询、来源追溯接入并完成黑盒与 UI 验收后，再安排本节。当前仅记录排期，尚未提供废品库存。

- [ ] 明确废品库存身份、库位／状态及单据所有权：区分成品报废与原材料损坏，仅接收实际交接的废品，已销毁或遗失部分不入库；不将全部报废登记量自动转换为废品库存。
- [ ] 提供“选择任务 → 关联批准清单中的报废来源 → 填写实际交接数量 → 仓管确认废品入库”入口。明确工序报废与结案新增报废的来源关系；原材料损坏关联独立损耗事实，不能混入成品报废数量。
- [ ] 按来源核算已接收量和剩余可接收量，保留任务、来源单据、批准版本及交接记录，防止同一事实跨来源或重复点击重复入库；明确清单更正和已入废品的处理规则后再实现。
- [ ] 复用唯一 `inventory_transaction` 库存账本，废品不得作为合格成品或普通可用物料参与分配。实现前同步范围、架构、数据库所有者文档与版本化 migration；入库及后续处理的具体规则另行定稿。

## 库存查询待办

### 日常库存数量查询改用余额投影

- 将分配候选、分配/出库锁内校验、盘点候选、创建和确认中的现存量流水聚合，改为读取现有 `inventory_batch_balance` 或 `inventory_material_variant_balance`。基础物料合计从版本余额按 `material_id` 汇总；物料库存页已读取余额投影，不再列入历史流水增长专项评估。
- 替换须保留精确版本、库存批次和库存状态条件，并验证批次状态过滤、缺行按零处理、流水与投影同事务维护、并发锁序、当前读及数量一致性。不得仅替换 SQL 而削弱写事务中的库存不足和并发校验。
- 完成标准：日常现存量查询与写入校验不再依赖历史流水求和，相关事务和数量验证通过。完成后移除“日常库存查询随历史流水增长”的专项评估事项；剩余容量工作按实际查询和并发瓶颈安排。

### 预留量查询性能检查

- 单独检查库存批次列表、分配和领料候选中的预留汇总。预留来自分配及出库明细，不等于账面库存余额，不能直接用现有余额投影替代。
- 按批次数、历史分配与出库量及并发量逐级增加数据规模，记录执行计划、扫描行数、耗时和锁等待，优先优化重复聚合与组合索引；分页返回少量结果不代表聚合只处理少量历史。
- 是否新增预留数量投影，待容量证据明确后决定，不预建影子表或第二套库存账本。测试集按黑盒确认后的测试阶段安排。

### 流水查询与事实来源边界

- `inventory_transaction` 始终是唯一库存事实来源，余额是同事务维护、可重建的派生结果。日常数量读取改用余额，只改变查询路径，不改变数据所有权或事实来源。
- 历史追溯按业务条件分页读取流水；对账和重建按各自范围聚合流水，不要求每次业务操作重新计算历史。相应查询按用途检查索引和执行成本，不与日常现存量查询混为一个性能评估项。
- `item_id` 是基础物料身份而非流水唯一键；精确库存使用 `material_variant_id`，批次操作还保留 `batch_id` 和库存状态。跨版本合计只能表示基础物料总量，不能用于替代版本或判断齐套；BOM 版本不代替库存精确版本身份。

## 产品复制快捷入口：已批准待实施

- [ ] 提供“复制为新产品”和“复制路线”的快捷操作，仍创建独立草稿并要求新编码和人工复核，不覆盖已批准BOM或历史路线；具体字段、权限和发布约束实施前同步Product所有者文档。原始阶段C的分类扁平化争议另见[CO-01](documentation-conflicts.md#co-01)，不随复制入口默认批准。

## 已识别方向（尚未立项）

采购到入库的一期验收见上方。本节记录更广范围的业务缺口；这些场景仍未立项，不表示其范围、所有权和实施方案已经批准。

### 通用 Inventory

- 待补场景：上述 Production 已确认的成品手动入库之外，自产半成品入库、委外完成入库、销售发货出库、仓库内库存报废，以及可用、待检、冻结和不良品之间的状态转换仍须另行明确范围。
- 实施前提：新通用场景须单独确定来源身份、业务所有权及公开命令边界，复用已有 Inventory，不拆出第二套流水或复制库存事实。
- 验收边界：业务单据确认、库存流水、余额投影和成功审计必须同事务；新增场景完成契约、追加 migration、并发/幂等约束及真实 MySQL 验证。当前设计见 [Inventory](../apps/api/src/modules/inventory/README.md) 与 [Production 数据库设计](../apps/api/src/modules/production/docs/database/README.md)。

### Quality

- 待定义场景：过程检验、完整最终检验、返工、报废或让步接收等更广场景不自动纳入现有窄 Quality。
- 后续抽样配置：按来料数量区间配置样本数量或比例，允许业务调整，不将档位和比例写死在代码中。检验保留实际覆盖量、样本数和采用的规则版本或参数，后续配置变化不改历史记录或已确认入库额度；配置能力另行安排，不因当前支持手工抽检就宣称已经实现。
- 实施前提：新增质量场景先批准范围，再确定检验批、检验项、抽样、结论及放行的事实来源，不将工序报工或已有批准产出复制为第二套质量事实。
- 验收边界：完成模块所有权、契约、追加 migration、权限、事务审计和测试前，不开放质量状态或以预留字段宣称能力已实现。当前范围以[产品范围](product-scope.md)为准。

### 全链路 Traceability

- 目标场景：从成品批次或销售交付向后定位生产、质量、原料批次和供应商，也能从原料批次向前定位受影响的生产批次、成品和交付对象。
- 实施顺序：先稳定通用 Inventory、Quality、成品入库和销售出库的数据模型，同时持续保留不可变事实、来源 ID、业务引用和历史快照；再建设跨模块追溯读模型，避免对未定稿表结构反复返工。
- 验收边界：先批准查询范围、数据所有权、关联键和权限；追溯只能聚合既有事实，不得创建第二库存、生产需求或质量事实来源。

## 审批试探验收与后续扩展

- 当前接入 BOM、生产需求更正和批次收尾场景，按角色、指定用户或场景业务来源配置顺序多级审批；当前使用入口、表结构和事务职责见 [Approval](../apps/api/src/modules/approval/README.md)。长期业务决策见 [ADR-0006](adr/0006-approval-workflow-boundaries.md) 和 [ADR-0008](adr/0008-approval-node-assignees.md)，未实施扩展见[审批与通知设计](approval-design.md)。
- 工单审批后续接入：沿用现有研发/批量工单，核对所选产品与外部订单要求、数量和交期；BOM 审批与工单审批独立。工单审批关联字段仍是所有者文档中的未来结构，不因规划改变当前工单下达状态机。
- Notification 独立提供站内通知，Approval 是首个调用方；同事务落库、非阻塞空钩子及当前验收项见下方“通用通知验收”，不在审批模块内建设专用外部发送能力。
- 通用审批决定的 HTTP 幂等重放仍待单独设计；目前决定使用版本、锁与唯一约束防重，不自动重试。Production 新增业务送审已纳入其幂等命令，与申请和绑定共事务。
- 新增审批场景时扩展明确的对象/证据契约、详情展示和接口权限入口，复用通用提交引擎；当前详情契约明确覆盖 BOM、需求更正和批次收尾。列表已经在分页前过滤实时资格，但仍逐申请读取节点并解析候选人，后续按实际查询量评估批量读取，须保留实时资格与实际处理历史可见性的区别。
- 外部订单核对是否需要附件或引用信息仍待明确；不据此提前增加采购审批或第二套生产需求。

## 通用通知验收

长期边界见 [ADR-0007](adr/0007-general-notification-boundaries.md) 和[通知设计](notification-design.md)，当前发布契约、接口、权限与数据库规则由 [Notification](../apps/api/src/modules/notification/README.md) 所有。Approval 是首个实际调用方，其他业务场景仍须单独批准。

- [ ] 用户确认设计并明确进入正式测试阶段后，由 Luna MAX 完成全仓库验证：执行 `corepack pnpm verify` 及 `corepack pnpm --filter @company/api typecheck:test`，处理剩余失败并核对最终结果。测试覆盖与执行边界见[测试策略](testing-strategy.md)及 Notification 所有者文档。
- [ ] 同一正式测试阶段在独立测试库完成 `corepack pnpm test:production:mysql` 全量回归，核对通知并发去重与首次已读、审批通知联动、事务及钩子隔离、迁移 up/down/up 与非空回滚保护；修复失败后取得完整通过结果。环境门禁见[跨模块测试](../tests/README.md)。
- 公告发布入口、独立中心页、人工定向发布的收件范围，以及非 Approval 的实际通知场景未批准；首期仅保留通用纯文本发布能力，不预建公告管理、定时提醒、全部已读、多标签页同步或清理归档。

### 实际接入外部渠道时再设计

- 可靠投递：是否要求宕机补发、可接受延迟与重复；据此定义同事务持久化投递记录或 outbox、幂等、重试退避、失败人工处置、多实例领取和顺序要求。不能只依赖提交后的内存钩子承诺送达。
- 渠道与账号：渠道适配端口、用户与外部账号绑定、凭证配置及脱敏、渠道停用或账号解绑后的处理、送达回执的含义；外部送达不自动变成站内已读。
- 消息量与打扰：超时、并发和积压上限、速率限制、过载策略、去重合并；按实际需求决定优先级、订阅偏好、免打扰、摘要和催办，不能让可关闭提醒影响业务待办本身。
- 运维与内容：失败/积压指标和关联追踪、重发权限及审计；渠道文案和模板版本仅在实际需要时设计，不复制完整业务单据，不预建通用模板平台。

## 最低优先级优化（当前不实施）

### F9：数据库表与字段中文 COMMENT

- 这是数据库客户端可读性的优化项，不属于当前功能缺陷修复。当前开发阶段数据库结构仍不稳定，暂不实施中文 COMMENT 补充，也不为此追加 migration。
- 待结构稳定后再安排：依据各表所有者文档核对中文表注释和字段说明，通过追加 migration 落地，并确保不改变字段类型、NULL、默认值、生成表达式及约束；不得修改已执行迁移。
- 优先级最低，不阻塞当前业务开发、数据库设计文档维护或黑盒验收。

## 待决策

未解决的定义冲突和实现差异集中在[冲突索引](documentation-conflicts.md)，各项保留当前状态及证据；未裁决内容不能自动作为新业务规则。

- 尚未批准的盘点专用冻结方案、权限层级、SOP预览、报工管理范围及未复现反馈见[待询问提案](../temp/--待询问.md)；询盘评估后研发占号设想见[后续方向](../temp/--之后需要做的一些重点方向.md)。这些入口保留独有理由和疑问，不作为开发授权。
- 物料改名审批：后续确定申请、审核权限和审计流程，当前名称编辑不代表审批已落地。

- 工序步骤 SOP 生命周期的重新设计尚未批准；在决策前不得据此新增表或接口。
- 是否从管理端拆出独立员工/检测工作站尚未批准。当前员工生产操作仍由 admin-web 所有；在目标用户、权限边界和独立发布需求明确前，不创建占位应用目录。

## 标签切换性能：待验收

既有业务黑盒反馈未覆盖本节 UI／性能专项或旧测试夹具适配，下列事项继续待验收。

- [ ] 使用实际数据固定标签反复切换，核对停止操作及关闭标签后 GC 的堆内存、CPU、DOM 和事件监听器；同时比较禁用浏览器扩展的环境，定位截图中约 1 GB 堆占用的保留路径。
- [ ] 黑盒核对报工记录／追溯的首次单次加载、快速离开取消读取、同批次刷新、跨批次响应乱序、报工刷新失败禁止沿用旧依据，以及弹窗草稿切换后恢复。用户确认后再补正式测试集，包括主动取消不重试和激活调度的边界。
- [ ] 核对返回页面 150 ms 内立即手动刷新时的请求体验：手动请求立即执行，目前不会合并待执行的自动刷新，后者到点会替代前一读取；若实际造成重复加载，再统一调度入口，保留手动操作即时执行及最新请求生效的规则。

## 生产退料、人工追加与返工：专项验收及测试

- [ ] 余料退回黑盒验收：短批未开工、已领料、执行中和结束后，部分／全部退料均不创建或恢复需求、不改变物料计划版本及短批授权；未满足需求余额不增、已满足需求不重开、候选无退料衍生缺口。核对开工预览／按钮／后端资格不变、公共库存可重新分配、退料与损耗互斥额度及提示。确认后再补正式测试。
- [ ] 人工追加后续履约专项测试待用户通知：既有记录载明普通领料后／开工后追加的黑盒反馈通过，不据此认定全部分支验收。覆盖分配、释放、制单、确认，追加未满足阻断完工、满足后可完工且不增补产额度；短批开工前重新授权／齐套续领、开工后普通需求与追加混合领用；工序报废补料与领料损坏事实保持独立，不恢复旧自动损耗补料断言。
- [ ] 返工目标黑盒验收：计划10＋下游报废补产2且补料齐套，上游目标12；返工恢复后正常累计12可完成，10／11不能提前完成。覆盖无补产、补料未齐套、来源工序自身报废不增自身目标、超当前目标拒绝；返工不新增投入、物料需求或补产授权。确认后再补正式测试。
- [ ] 正式测试阶段补主数据编码不可改、半成品按物料查询、现行 ProductsPage／ProductFormDialog／ProductionTasksPage及需求弹窗；补初配／追加与版本并发停用、幂等重放与同键内容冲突。沿用当前名称展示，编码、单位、精确版本保持固化；批量报废补料遵守任务锁版，损耗不自动提需，不采用旧跨版本补料断言。

## 已确认缺陷：待修复

以下为已确认但尚未修复的缺陷，完成后按统一交付顺序验证。

- [ ] AuthClient会话竞态：按会话版本约束refresh成功／失败清理、401重试及me用户资料回写，finally仅清理自身持有的刷新Promise。验收退出A并登录B后，A旧刷新成功／失败、旧401或用户资料晚到均不修改B，也不使用B凭证重试旧请求；同会话合法并发继续可用。
- [ ] SOP候选只取第1页100条：修复 `useProductionBatchesList` 的候选完备性，取齐分页或提供远程搜索与已选ID解析；验收101条以上文件和历史已选回显。
- [ ] 缓存任务页未消费新定位：从出库阻断跳转时，ProductionTasksPage需应用新的route.query.keyword、重置分页并查询，不能仅在onMounted处理；保留尚未提交草稿，当前可手动搜索。
- [ ] HTTP超时提示优先级：`handleHttpError`先判status=0，ECONNABORTED被提示为普通网络错误；按[错误处理边界](../apps/admin-web/docs/http-error-handling.md#121-超时当前会优先被识别为网络错误)区分超时，验收后补相邻用例。
