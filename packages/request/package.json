{
  "name": "@company/request",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "corepack pnpm --workspace-root exec turbo run build:run --filter=@company/request",
    "build:run": "tsc -p tsconfig.json",
    "typecheck": "corepack pnpm --workspace-root exec turbo run typecheck:run --filter=@company/request",
    "typecheck:run": "tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm --workspace-root exec turbo run test:run --filter=@company/request",
    "test:run": "vitest run --config ../../vitest.config.ts src"
  },
  "dependencies": {
    "@company/constants": "workspace:*",
    "axios": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
