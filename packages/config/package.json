{
  "name": "@company/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "corepack pnpm --workspace-root exec turbo run build:run --filter=@company/config",
    "build:run": "tsc -p tsconfig.json",
    "typecheck": "corepack pnpm --workspace-root exec turbo run typecheck:run --filter=@company/config",
    "typecheck:run": "tsc -p tsconfig.json --noEmit",
    "test": "corepack pnpm --workspace-root exec turbo run test:run --filter=@company/config",
    "test:run": "vitest run --config ../../vitest.config.ts src"
  },
  "dependencies": {
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
